<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moving Robot</title>
    <style>
        body {
            margin: 0;
            overflow: hidden;
            background-color: #f0f0f0;
        }

        .robot {
            position: absolute;
            width: 100px;
            height: 150px;
            animation: moveRobot 5s linear infinite; /* Continuous movement */
        }

        /* Robot's head */
        .head {
            position: relative;
            width: 50px;
            height: 50px;
            background: #555;
            border-radius: 10px;
            margin: 0 auto;
        }

        .head::before, .head::after {
            content: '';
            position: absolute;
            width: 10px;
            height: 10px;
            background: white;
            border-radius: 50%;
            top: 15px;
        }

        .head::before {
            left: 10px; /* Left eye */
        }

        .head::after {
            right: 10px; /* Right eye */
        }

        /* Robot's body */
        .body {
            width: 60px;
            height: 80px;
            background: #777;
            border-radius: 5px;
            margin: 10px auto;
        }

        /* Robot's arms */
        .arm {
            position: absolute;
            width: 20px;
            height: 50px;
            background: #888;
            top: 40px;
        }

        .left-arm {
            left: -20px;
        }

        .right-arm {
            right: -20px;
        }

        /* Robot's legs */
        .leg {
            width: 20px;
            height: 30px;
            background: #444;
            margin: 0 10px;
            display: inline-block;
        }

        @keyframes moveRobot {
            0% {
                left: -150px;
                top: 200px;
            }
            100% {
                left: 100%;
                top: 200px;
            }
        }
    </style>
</head>
<body>
    <div class="robot">
        <div class="head"></div>
        <div class="body"></div>
        <div class="arm left-arm"></div>
        <div class="arm right-arm"></div>
        <div>
            <div class="leg"></div>
            <div class="leg"></div>
        </div>
    </div>
</body>
</html><!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moving Robot</title>
    <style>
        body {
            margin: 0;
            overflow: hidden;
            background-color: #f0f0f0;
        }

        .robot {
            position: absolute;
            width: 100px;
            height: 150px;
            animation: moveRobot 5s linear infinite; /* Continuous movement */
        }

        /* Robot's head */
        .head {
            position: relative;
            width: 50px;
            height: 50px;
            background: #555;
            border-radius: 10px;
            margin: 0 auto;
        }

        .head::before, .head::after {
            content: '';
            position: absolute;
            width: 10px;
            height: 10px;
            background: white;
            border-radius: 50%;
            top: 15px;
        }

        .head::before {
            left: 10px; /* Left eye */
        }

        .head::after {
            right: 10px; /* Right eye */
        }

        /* Robot's body */
        .body {
            width: 60px;
            height: 80px;
            background: #777;
            border-radius: 5px;
            margin: 10px auto;
        }

        /* Robot's arms */
        .arm {
            position: absolute;
            width: 20px;
            height: 50px;
            background: #888;
            top: 40px;
        }

        .left-arm {
            left: -20px;
        }

        .right-arm {
            right: -20px;
        }

        /* Robot's legs */
        .leg {
            width: 20px;
            height: 30px;
            background: #444;
            margin: 0 10px;
            display: inline-block;
        }

        @keyframes moveRobot {
            0% {
                left: -150px;
                top: 200px;
            }
            100% {
                left: 100%;
                top: 200px;
            }
        }
    </style>
</head>
<body>
    <div class="robot">
        <div class="head"></div>
        <div class="body"></div>
        <div class="arm left-arm"></div>
        <div class="arm right-arm"></div>
        <div>
            <div class="leg"></div>
            <div class="leg"></div>
        </div>
    </div>
</body>
</html>
